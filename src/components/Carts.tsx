import React, { ReactElement } from 'react';
import '../styles/_carts.scss';

interface Props {
  
}

export default function Carts({}: Props): ReactElement {
  return (
    <div className="container">
      <h1>i am from cart components page</h1>
    </div>
  )
}
